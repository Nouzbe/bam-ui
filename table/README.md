This is a pure presentational React component, able to render a table.

It accepts the following props:

* initialConfiguration (uncontrolled)
* onConfigurationChange (uncontrolled)

* configuration (controlled)

---

* data